{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Docker: Attach to Node",
            "internalConsoleOptions": "neverOpen",
            "address": "localhost",
            "port": 9229,
            "remoteRoot": "/app",
            "localRoot": "${workspaceFolder}",
            "restart": true
        },
    ]
}